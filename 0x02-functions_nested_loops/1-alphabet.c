#include "main.h"

/**
 * print_alphabet - print all alphabet lowercase
 */
void print alphabet (void)
{
        char letter;
       for(letter = 'a' ; letter <= 'z'; letter++)
          _alphabet(letter);

          _alphabet('\n'); 	  
}
